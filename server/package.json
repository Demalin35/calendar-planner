{
  "name": "calendar-planner-server",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Server sources live here; production install/build/start is from the repository root.",
  "scripts": {
    "dev": "tsx watch index.ts",
    "typecheck": "tsc -p tsconfig.build.json --noEmit"
  }
}
